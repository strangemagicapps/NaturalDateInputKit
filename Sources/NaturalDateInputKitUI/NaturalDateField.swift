import SwiftUI
import NaturalDateInputKit

/// A SwiftUI text field that interprets natural-language date and time input using
/// ``NaturalDateInputKit/NaturalDateParser``.
///
/// > Important: This is a placeholder stub. The full implementation will land in a
/// > later release.
public struct NaturalDateField: View {
    /// Creates a new field with no bound state.
    public init() {}

    public var body: some View {
        EmptyView()
    }
}
